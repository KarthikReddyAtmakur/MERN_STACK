export function add(x, y) {
  console.log(x + y);
}

export function subtract(x, y) {
  console.log(x - y);
}
// export {add,subtract}
/* export function add(x, y) {
  console.log(x + y);
} can import the function without {} => import add from ".calc.js",
works only when their is only one function */
